name: Recompute ROI

on:
  workflow_dispatch:
  schedule:
    - cron: '5 8 * * *'

permissions:
  contents: write

env:
  PYTHONPATH: .

jobs:
  recompute:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch eBay OAuth token
        run: python scripts/get_ebay_token.py

      - name: Build market-ready input
        run: >-
          python scripts/build_market_ready.py
          --deals input/canadiantire_all_liquidations.json
          --keepa input/keepa_sales.json
          --out input/market_ready.json

      - name: Recompute ROI
        run: python scripts/recompute_roi.py --input input/market_ready.json --output output/marketplace.json --top 300

      - name: Commit results
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if git diff --quiet -- output/marketplace.json; then
            echo "No changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/marketplace.json
          if ! git diff --quiet -- input/market_ready.json; then
            git add input/market_ready.json
          fi
          git commit -m "chore: update marketplace [skip ci]"
          git push
